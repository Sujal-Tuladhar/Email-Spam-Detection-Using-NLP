# =====================================================================
# Email Spam Detection using NLP - Python dependencies
# Tested with Python 3.10 / 3.11
# Install with:  pip install -r requirements.txt
# =====================================================================

# --- Core data + visualisation ---
numpy>=1.24,<2.1
pandas>=2.0,<2.3
matplotlib>=3.7,<3.10
seaborn>=0.12,<0.14

# --- Classical ML (SVM + Random Forest + TF-IDF) ---
scikit-learn>=1.3,<1.6

# --- NLP preprocessing (stopwords, lemmatization, tokenisation) ---
nltk>=3.8,<3.10

# --- Word-cloud visualisation in EDA ---
wordcloud>=1.9,<1.10

# --- HuggingFace dataset loader (SetFit/enron_spam) ---
datasets>=2.18,<3.1

# --- DistilBERT fine-tuning ---
transformers>=4.40,<4.50
accelerate>=0.30,<1.1

# --- Deep-learning backend for transformers ---
# Pick ONE of the two lines below depending on your hardware:
#   GPU (CUDA 12.x):  pip install torch --index-url https://download.pytorch.org/whl/cu121
#   CPU only:         pip install torch
torch>=2.2,<2.5

# --- Notebook runtime ---
jupyter>=1.0
notebook>=7.0
ipywidgets>=8.0
